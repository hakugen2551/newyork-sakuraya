{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo '🔄 GitHub から最新版を取得中...' && git pull --rebase --autostash && echo '✅ 最新の状態で作業を開始できます' || echo '⚠️ git pull に失敗しました。ネット接続やコンフリクトを確認してください'"
          }
        ]
      }
    ]
  }
}
